# Server Configuration
PORT=3001
NODE_ENV=development

# Bedrock Agent Mode (set to true for real AI)
USE_BEDROCK=false

# AWS Bedrock Agent Configuration (for real Bedrock Agent integration)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here

# Bedrock Agent Configuration
BEDROCK_AGENT_ID=AJBHXXILZN
BEDROCK_AGENT_ALIAS_ID=AVKP1ITZAA

# Frontend Configuration
FRONTEND_URL=http://localhost:3000

# Optional: MongoDB (not required for basic functionality)
# MONGODB_URI=mongodb://localhost:27017/rummy-assistant

# =====================================
# HACKATHON SETUP INSTRUCTIONS:
# =====================================
# 1. Copy this file to .env
# 2. For DEMO MODE: Keep USE_BEDROCK=false (current setup)
# 3. For REAL AI: Set USE_BEDROCK=true and add AWS credentials
# 4. Get AWS credentials from: AWS Console → IAM → Users → Access Keys
# 5. Configure your Bedrock Agent ID and Alias ID in AWS Console
# ===================================== 